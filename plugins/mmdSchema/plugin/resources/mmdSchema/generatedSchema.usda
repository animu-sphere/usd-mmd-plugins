#usda 1.0
(
    "WARNING: THIS FILE IS GENERATED BY usdGenSchema.  DO NOT EDIT."
)

class "MmdMaterialAPI" (
    customData = {
        string userDocBrief = "The canonical semantics of one PMX material."
    }
)
{
    color3f inputs:mmd:material:ambientColor = (0, 0, 0) (
        customData = {
            string userDocBrief = "PMX ambient RGB."
        }
    )
    uniform bool inputs:mmd:material:castSelfShadow = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x04, draws into the self-shadow map."
        }
    )
    color4f inputs:mmd:material:diffuseColor = (1, 1, 1, 1) (
        customData = {
            string userDocBrief = "PMX diffuse RGBA."
        }
    )
    uniform bool inputs:mmd:material:doubleSided = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x01, no culling."
        }
    )
    uniform bool inputs:mmd:material:drawEdge = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x10, draws the outline."
        }
    )
    uniform bool inputs:mmd:material:drawLines = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x80."
        }
    )
    uniform bool inputs:mmd:material:drawPoints = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x40."
        }
    )
    color4f inputs:mmd:material:edgeColor = (0, 0, 0, 0) (
        customData = {
            string userDocBrief = "PMX edge RGBA, authored whether or not drawEdge is set."
        }
    )
    float inputs:mmd:material:edgeSize = 0 (
        customData = {
            string userDocBrief = "PMX edge size, authored whether or not drawEdge is set."
        }
    )
    uniform bool inputs:mmd:material:groundShadow = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x02, ground shadow."
        }
    )
    uniform bool inputs:mmd:material:receiveSelfShadow = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x08, receives self shadow."
        }
    )
    uniform int inputs:mmd:material:sharedToonIndex = -1 (
        customData = {
            string userDocBrief = """MMD's shared toon slot 0-9 (toon01.bmp-toon10.bmp), authored
        only when toonSource is shared; -1 when unauthored."""
        }
    )
    color3f inputs:mmd:material:specularColor = (0, 0, 0) (
        customData = {
            string userDocBrief = "PMX specular RGB."
        }
    )
    float inputs:mmd:material:specularPower = 0 (
        customData = {
            string userDocBrief = "PMX specular power (exponent)."
        }
    )
    uniform token inputs:mmd:material:sphereMode = "disabled" (
        allowedTokens = ["disabled", "multiply", "add", "subTexture"]
        customData = {
            string userDocBrief = "How the sphere texture combines."
        }
    )
    asset inputs:mmd:material:sphereTexture = @@ (
        customData = {
            string userDocBrief = "The sphere texture, when the PMX slot names a safe asset path."
        }
    )
    asset inputs:mmd:material:texture = @@ (
        customData = {
            string userDocBrief = "The base texture, when the PMX slot names a safe asset path."
        }
    )
    uniform token inputs:mmd:material:toonSource = "none" (
        allowedTokens = ["none", "individual", "shared"]
        customData = {
            string userDocBrief = """Which toon ramp the material names: none; individual, in
        toonTexture; or shared, MMD's own ramp sharedToonIndex, whose image
        the consumer supplies."""
        }
    )
    asset inputs:mmd:material:toonTexture = @@ (
        customData = {
            string userDocBrief = """The individual toon texture, authored only when toonSource is
        individual and the path is safe."""
        }
    )
    uniform bool inputs:mmd:material:vertexColor = 0 (
        customData = {
            string userDocBrief = "PMX drawing flag 0x20."
        }
    )
}

